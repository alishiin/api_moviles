# Nombre de la aplicación

spring.application.name=ApiMoviles

# Configuración de PostgreSQL (Supabase)

spring.datasource.url=jdbc:postgresql://db.lfxnmmnafctoppbicrer.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=wwe1213zX
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración HikariCP

spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# Configuración JPA/Hibernate

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Puerto del servidor

server.port=8080

# Logging

logging.level.com.example.ApiMoviles=DEBUG
